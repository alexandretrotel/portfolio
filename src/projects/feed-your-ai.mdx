Feed Your AI is a tool to combine text files for AI processing with filtering options. The goal is to make it simple to give the whole repository context to the AI, while also allowing for filtering of files that are not relevant to the current task.
