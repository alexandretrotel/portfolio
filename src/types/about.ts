export type Experience = {
  title: string;
  subtitle: string;
  date: string;
  url?: string;
  image?: string;
  disabled?: boolean;
  readMore?: string;
}[];

export type Education = {
  title: string;
  subtitle: string;
  date: string;
  url?: string;
  image?: string;
  disabled?: boolean;
}[];
